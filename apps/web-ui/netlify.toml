[build]
  command = "npm run build:netlify"
  publish = "apps/web-ui/.next"

# Single-page app: serve index.html for any path (e.g. direct links, refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
