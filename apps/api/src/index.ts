/**
 * @guardrail/api package entry — re-exports the canonical factory and runtime starter.
 * Run the server via `pnpm dev` / `pnpm start` (executes start.ts).
 */
export { buildServer } from "./server";
export { start } from "./start";
